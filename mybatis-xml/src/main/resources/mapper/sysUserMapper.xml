<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bat.ibatis.dao.SysUserDao">

    <resultMap id="sysUserMap" type="SysUserPO">
        <!-- 指定主键的字段-->
        <id property="userUuid" column="userUuid"/>
        <collection property="sysRoleList" ofType="SysRolePO" select="com.bat.ibatis.dao.SysRoleDao.getSysRoleByUserUuid" column="{userUuid = userUuid}" fetchType="lazy"/>
    </resultMap>

    <select id="getSysUserList" resultMap="sysUserMap">
        select s1.userUuid, s1.username, s1.password, s1.email, s1.phone, s1.createTime, s1.updateTime from sys_user s1
        <where>
            <if test="condition.accountName != null and condition.accountName != ''">
                and s1.username = #{condition.accountName} or s1.email = #{condition.accountName} or s1.phone =
                #{condition.accountName}
            </if>

            <if test="condition.username != null and condition.username != ''">
                and s1.username like CONCAT('%',#{condition.username},'%')
            </if>

            <if test="condition.userUuidList != null and condition.userUuidList.size > 0">
                and s1.userUuid in
                <foreach collection="userUuidList" open="(" close=")" separator="," item="userUuid" index="i">
                    #{condition.userUuid}
                </foreach>
            </if>
        </where>
    </select>

    <insert id="insertSysUser" parameterType="SysUserPO">
        insert into sys_user (userUuid, username, password, email, phone, createTime) values (#{userUuid}, #{username}, #{password}, #{email}, #{phone}, #{createTime})
    </insert>

    <insert id="batchInsertSysUser">
        insert into sys_user (userUuid, username, password, email, phone, createTime) values
        <foreach collection="sysUserList" item="sysUser" separator=",">
            (#{sysUser.userUuid}, #{sysUser.username}, #{sysUser.password}, #{sysUser.email}, #{sysUser.phone},
            #{sysUser.createTime})
        </foreach>
    </insert>

    <update id="updateSysUser" parameterType="SysUserPO">
        update sys_user
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>

            <if test="password != null and password != ''">
                password = #{password},
            </if>

            <if test="email != null and email != ''">
                email = #{email},
            </if>

            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>

            <if test="updateTime != null and updateTime != ''">
                updateTime = #{updateTime},
            </if>
        </set>
        where userUuid = #{userUuid}
    </update>
</mapper>