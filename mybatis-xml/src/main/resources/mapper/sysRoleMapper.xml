<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bat.ibatis.dao.SysRoleDao">

    <resultMap id="sysRoleMap" type="SysRolePO">
        <result property="roleUuid" column="role_uuid"/>
        <result property="roleName" column="role_name"/>
        <result property="enabled" column="enabled"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="getSysRoleByUserUuid" resultMap="sysRoleMap">
        select s1.role_uuid, s1.role_name, s1.enabled, s1.create_time, s1.update_time from sys_role s1
        inner join sys_user_role_rel s2 on s2.role_uuid = s1.role_uuid
        where s2.user_uuid = #{userUuid}
    </select>

    <insert id="insertSysRole" parameterType="SysRolePO">
        insert into sys_role (role_uuid, role_name, enabled, create_time) values (#{roleUuid}, #{roleName}, #{enabled}, #{createTime})
    </insert>

</mapper>