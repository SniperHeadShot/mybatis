<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bat.ibatis.dao.SysRoleDao">

    <select id="getSysRoleList" resultType="SysRolePO">
        select roleUuid, roleName, createTime, updateTime from sys_role
    </select>

    <insert id="insertSysRole" parameterType="SysRolePO">
        insert into sys_role (roleUuid, roleName, createTime) values (#{roleUuid}, #{roleName}, #{createTime})
    </insert>
</mapper>